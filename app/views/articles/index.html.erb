<div class="container">
<h1>Articles</h1>
    <%@articles.each do |article| %>
    <div class="card">
        <h2><li><%= link_to article.title, article_path(article) %></li></h2>
        <p><%= article.content %></p>
    </div>
    <% end %>
</div>
